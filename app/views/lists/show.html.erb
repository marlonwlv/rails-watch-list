
<%= render 'banner' %>

<%= link_to 'Add movie', new_list_bookmark_path(@list), class:"btn btn-flat" %>


<div class="container">
  <div class="cards">
    <% @movies.each do |movie| %>
      <div class="card-trip">
        <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
        <div class="card-trip-infos">
          <div>
            <h2><%= movie.title %></h2>
            <p><%= movie.rating %> </p>
          </div>
          <p class="card-trip-pricing"><%= movie.overview %></p>
          <div>
            <p><%= movie.overview %></p>
            <a href="">delete</a>
          </div>
        </div>
      </div>
    <% end  %>
  </div>
</div>
